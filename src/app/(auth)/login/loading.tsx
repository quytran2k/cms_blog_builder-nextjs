const Loading = () => {
  return (
    <div className="w-screen h-screen flex items-center justify-center">
      <p className="font-primary text-2xl font-bold">Loading</p>
    </div>
  );
};

export default Loading;
